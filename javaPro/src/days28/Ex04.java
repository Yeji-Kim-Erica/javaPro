package days28;

/**
 * @author 김예지
 * @date 2025. 3. 13.
 * @subject [스트림의 연산]
 * @content		ㄴ 스트림이 제공하는 다양한 연산을 이용해서
 				  복잡한 작업들을 간단히 처리하는 기능(== 함수, 메서드)
 				ㄴ 연산(operation)? 스트림에 정의된 메서드 중에 데이터 소스를 다루는 메서드
 				  s1.distinct().limit(6).sorted().forEach()
 				  	 중간 연산자	  중			중	  최종 연산자
 				  * 지연된 연산
 				  	최종 연산이 호출될 때 중간연산이 함께 호출됨 (나중에 호출된다 -> 지연된 연산이라고 함)
 				  1) 중간 연산: 연산 결과가 스트림
 				  2) 최종 연산: 연산 결과가 스트림이 아님
 				  
 				  예시) Ex04_02.java
 */
public class Ex04 {

	public static void main(String[] args) {
		
		
		
	} // main
	
} // class
